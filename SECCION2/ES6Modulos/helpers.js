
//ejemplo de export en linea

//export const sum = (a, b)=> a + b;
//export const mul = (a, b)=> a * b;

//ejemplo de export en Genral 

const sum = (a, b)=> a + b;
const mul = (a, b)=> a * b;

export{sum,mul}